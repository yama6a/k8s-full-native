apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: root
  namespace: sys-fluxcd
spec:
  interval: 1m
  chart:
    spec:
      chart: ./k8s/root
      sourceRef:
        kind: GitRepository
        name: my-cluster
        namespace: sys-fluxcd
  values:
    env: stage
